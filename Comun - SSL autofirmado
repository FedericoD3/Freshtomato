# El certificado autofirmado es inseguro porque equivale a decir uno mismo "sí soy yo" en lugar de que lo haga un ente autorizado
#  por lo tanto el navegador se queja, aunque la trasmisión si vaya encriptada.
# Entes como Let's Encrypt son muy complicados sin tener dominio propio real (en lugar de xxx.duckdns.org o zzz.noip.net)
#  y además muchos no están disponibles para Venezuela

# Crear la llave privada y la llave pública, y guardarlos en un directorio determinado, accesible a nginx:
  sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /algun/dir/determinado/nginx-selfsigned.key -out /algun/dir/determinado/nginx-selfsigned.crt

# Agregar SSL a la configuración del website en nginx:
    server {
      listen 80;                    # (o el puerto que sea que tengas configurado, 80 es el standard)
      server_name algo.dominio.com;
      location = ...
  # se convierte en:
    server {
      listen 443 ssl;               # (o el puerto que sea que tengas configurado, 443 es el standard)
      server_name algo.dominio.com;
      ssl_certificate     /algun/dir/determinado/nginx-selfsigned.crt;
      ssl_certificate_key /algun/dir/determinado/nginx-selfsigned.key;
      location = ...



# Agregar las llaves de encriptación a la configuración del website en nginx:
    location = / {
    root / ... o proxy_pass ...
  # se convierte en
    location = / {
 
    root / ... o proxy_pass ...

      
